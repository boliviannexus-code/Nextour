<nav class="account-tabs" aria-label="{{ __('Menú de cuenta turista') }}">
    <a href="{{ route('tourist.reservations.index') }}">{{ __('Reservas') }}</a>
    <a href="{{ route('tourist.history') }}">{{ __('Historial') }}</a>
    <a href="{{ route('tourist.profile') }}">{{ __('Perfil') }}</a>
</nav>
